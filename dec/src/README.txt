The design implements a DeC Router. 

Check global.v for the configuration and link format

top_dec.v:
  Top module of DeC router.
  
top_dec_2subnet:
  Top module of a node, assuming 2 sub-networks with 2 sub-routers